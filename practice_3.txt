CREATE TABLE Customers (
  customer_id INT PRIMARY KEY,
  first_name VARCHAR(100) NOT NULL,
  last_name VARCHAR(100),
  age INT CHECK (age > 18),
  country VARCHAR(100) DEFAULT 'Unknown'
);

CREATE TABLE Orders (
  order_id INT PRIMARY KEY,
  item VARCHAR(100) NOT NULL,
  amount INT CHECK (amount > 0),
  customer_id INT,
  CONSTRAINT unique_item UNIQUE (item, customer_id)
);

ALTER TABLE Orders
ADD CONSTRAINT fk_customer_order
FOREIGN KEY (customer_id) REFERENCES Customers(customer_id);

ALTER TABLE Customers
MODIFY age INT NOT NULL;

ALTER TABLE Orders
ADD CONSTRAINT check_amount CHECK (amount >= 100);

ALTER TABLE Customers
ALTER country SET DEFAULT 'USA';

ALTER TABLE Orders DROP CONSTRAINT check_amount;

ALTER TABLE Shippings ADD PRIMARY KEY(shipping_id);

ALTER TABLE Shippings
ADD CONSTRAINT fk_ship_customer
FOREIGN KEY (customer) REFERENCES Customers(customer_id);

CREATE TABLE Sample (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100)
);
